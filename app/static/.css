.patients-list-container {
    margin-top: 20px;
    background: white;
    border-radius: 8px;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    padding: 15px;
}
.patients-list {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
}
.patient-badge {
    background: #e6f2ff;
    color: #0066cc;
    padding: 5px 10px;
    border-radius: 4px;
    cursor: pointer;
    transition: background 0.2s ease;
}
.patient-badge:hover {
    background: #cce5ff;
}